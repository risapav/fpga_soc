// AUTO-GENERATED BY gen_config.py — DO NOT EDIT MANUALLY
// Generated: 2026-03-22 12:43:26
`default_nettype none

// --- Simple memory-mapped bus ---
interface bus_if;
  logic [31:0] addr;
  logic [31:0] wdata;
  logic [31:0] rdata;
  logic [3:0]  be;
  logic        we;
  logic        valid;
  logic        ready;
  modport master (output addr, wdata, be, we, valid,
                  input  rdata, ready);
  modport slave  (input  addr, wdata, be, we, valid,
                  output rdata, ready);
endinterface : bus_if

// --- AXI4-Lite ---
interface axi_lite_if #(
  parameter int AW = 32,
  parameter int DW = 32
);
  logic [AW-1:0] awaddr, araddr;
  logic [DW-1:0] wdata, rdata;
  logic [3:0]    wstrb;
  logic          awvalid, awready;
  logic          wvalid,  wready;
  logic          bvalid,  bready;
  logic [1:0]    bresp;
  logic          arvalid, arready;
  logic          rvalid,  rready;
  logic [1:0]    rresp;

  modport master (
    output awaddr, awvalid, wdata, wstrb, wvalid, bready,
           araddr, arvalid, rready,
    input  awready, wready, bresp, bvalid, arready,
           rdata, rresp, rvalid
  );
  modport slave (
    input  awaddr, awvalid, wdata, wstrb, wvalid, bready,
           araddr, arvalid, rready,
    output awready, wready, bresp, bvalid, arready,
           rdata, rresp, rvalid
  );
endinterface : axi_lite_if

// --- AXI4-Stream ---
interface axi_stream_if #(
  parameter int DW = 32,
  parameter int UW = 1
);
  logic [DW-1:0] tdata;
  logic [UW-1:0] tuser;
  logic          tvalid, tready, tlast;

  modport master (output tdata, tuser, tvalid, tlast, input tready);
  modport slave  (input  tdata, tuser, tvalid, tlast, output tready);
endinterface : axi_stream_if
