// AUTO-GENERATED BY gen_config.py - DO NOT EDIT MANUALLY
// Generated: {{ timestamp }}
`default_nettype none

  // Bus protocol (simple memory-mapped, PicoRV32 native):
  //   valid=1, we=1 -> write:  addr_i, wdata_i, we_i all valid, rdata_o ignored
  //   valid=1, we=0 -> read:   addr_i valid, rdata_o must be valid same cycle
  //   ready=1       -> transaction accepted (1-cycle latency assumed)
  //   No burst, no error signaling, 32-bit data, byte enables via bus.be

interface bus_if;
  logic [31:0] addr, wdata, rdata;
  logic [3:0]  be;
  logic        we, valid, ready;
  modport master (output addr, wdata, be, we, valid, input rdata, ready);
  modport slave  (input  addr, wdata, be, we, valid, output rdata, ready);
endinterface : bus_if

interface axi_lite_if #(parameter int AW=32, DW=32);
  logic [AW-1:0] awaddr, araddr;
  logic [DW-1:0] wdata, rdata;
  logic [3:0]    wstrb;
  logic awvalid, awready, wvalid, wready;
  logic bvalid, bready, arvalid, arready, rvalid, rready;
  logic [1:0] bresp, rresp;
  modport master (output awaddr, awvalid, wdata, wstrb, wvalid, bready,
                         araddr, arvalid, rready,
                  input  awready, wready, bresp, bvalid,
                         arready, rdata, rresp, rvalid);
  modport slave  (input  awaddr, awvalid, wdata, wstrb, wvalid, bready,
                         araddr, arvalid, rready,
                  output awready, wready, bresp, bvalid,
                         arready, rdata, rresp, rvalid);
endinterface : axi_lite_if

interface axi_stream_if #(parameter int DW=32, UW=1);
  logic [DW-1:0] tdata;
  logic [UW-1:0] tuser;
  logic tvalid, tready, tlast;
  modport master (output tdata, tuser, tvalid, tlast, input tready);
  modport slave  (input  tdata, tuser, tvalid, tlast, output tready);
endinterface : axi_stream_if
